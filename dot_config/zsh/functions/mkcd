# =============================================================================
# Make Directory and Change Into It
# Create a directory and change into it in one command
# =============================================================================
# Usage: mkcd <directory>
# Example: mkcd my-new-folder

if [[ $# -eq 0 ]]; then
  echo "Usage: mkcd <directory>" >&2
  return 1
fi
mkdir -p "$1" && cd "$1"
