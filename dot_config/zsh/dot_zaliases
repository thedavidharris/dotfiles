#!/bin/zsh
#
# Zsh aliases and functions
# Organized by category for better maintainability

# =============================================================================
# SYSTEM & PERMISSIONS
# =============================================================================

# Enable aliases to be sudo'ed
alias sudo='sudo '

# =============================================================================
# NAVIGATION
# =============================================================================

# Quick directory navigation
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias ~="cd ~" # `cd` is probably faster to type though
alias -- -="cd -"

# Modern directory navigation with zoxide
command -v z > /dev/null && alias cd="z"  # Use zoxide for cd if available
alias cdd="cd ~/Desktop"
alias cdl="cd ~/Downloads"
alias cdo="cd ~/Documents"

# =============================================================================
# UTILITIES & TOOLS
# =============================================================================

# Stopwatch
alias timer='echo "Timer started. Stop with Ctrl-D." && date && time cat && date'

# =============================================================================
# NETWORK & SYSTEM INFO
# =============================================================================

# IP addresses
alias ip="dig +short myip.opendns.com @resolver1.opendns.com"
alias localip="ipconfig getifaddr en0"
alias ips="ifconfig -a | grep -o 'inet6\? \(addr:\)\?\s\?\(\(\([0-9]\+\.\)\{3\}[0-9]\+\)\|[a-fA-F0-9:]\+\)' | awk '{ sub(/inet6? (addr:)? ?/, \"\"); print }'"

# Show active network interfaces
alias ifactive="ifconfig | pcregrep -M -o '^[^\t:]+:([^\n]|\n\t)*status: active'"

# View HTTP traffic
alias sniff="sudo ngrep -d 'en1' -t '^(GET|POST) ' 'tcp and port 80'"
alias httpdump="sudo tcpdump -i en1 -n -s 0 -w - | grep -a -o -E \"Host\: .*|GET \/.*\""

# =============================================================================
# SYSTEM UTILITIES & COMPATIBILITY
# =============================================================================

# Canonical hex dump; some systems have this symlinked
command -v hd > /dev/null || alias hd="hexdump -C"

# macOS compatibility aliases
command -v md5sum > /dev/null || alias md5sum="md5"
command -v sha1sum > /dev/null || alias sha1sum="shasum"

# Clipboard utilities
alias c="tr -d '\n' | pbcopy"

# URL-encode strings
alias urlencode='python -c "import sys, urllib as ul; print ul.quote_plus(sys.argv[1]);"'

# Ring the terminal bell, and put a badge on Terminal.app's Dock icon
alias badge="tput bel"

# Intuitive map function
alias map="xargs -n1"

# Reload the shell
alias reload="exec $SHELL -l"

# Print each PATH entry on a separate line
alias path='echo -e ${PATH//:/\\n}'

# =============================================================================
# APPLICATIONS
# =============================================================================

# Google Chrome
alias chrome='/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome'

# =============================================================================
# macOS SYSTEM MANAGEMENT
# =============================================================================

# File system cleanup
alias cleanup="find . -type f -name '*.DS_Store' -ls -delete"
alias emptytrash="sudo rm -rfv /Volumes/*/.Trashes; sudo rm -rfv ~/.Trash; sudo rm -rfv /private/var/log/asl/*.asl; sqlite3 ~/Library/Preferences/com.apple.LaunchServices.QuarantineEventsV* 'delete from LSQuarantineEvent'"

# Finder management
alias show="defaults write com.apple.finder AppleShowAllFiles -bool true && killall Finder"
alias hide="defaults write com.apple.finder AppleShowAllFiles -bool false && killall Finder"

# System controls
alias afk="/System/Library/CoreServices/Menu\ Extras/User.menu/Contents/Resources/CGSession -suspend"
alias cleardock="defaults write com.apple.dock persistent-apps -array \"\" && killall Dock"

# Spotlight management
alias spotoff="sudo mdutil -a -i off"
alias spoton="sudo mdutil -a -i on"

# =============================================================================
# DEVELOPMENT TOOLS
# =============================================================================

# Xcode commands
alias simctl="xcrun simctl"
alias plistbuddy="/usr/libexec/PlistBuddy"

# PDF utilities
alias mergepdf='/System/Library/Automator/Combine\ PDF\ Pages.action/Contents/Resources/join.py'

# =============================================================================
# FILE LISTING & MODERN TOOLS
# =============================================================================

# Traditional ls with colors
if ls --color > /dev/null 2>&1; then # GNU `ls`
  colorflag="--color=auto"
else # OS X `ls`
  colorflag="-G"
fi

# Tree with colors (if tree is installed)
command -v tree > /dev/null && alias tree="tree -C"

# Modern tool aliases (preferred when available)
if command -v eza >/dev/null 2>&1; then
  # Use eza (modern ls replacement)
  alias ls="eza --color=always --group-directories-first"
  alias la="eza -la --color=always --group-directories-first"
  alias ll="eza -l --color=always --group-directories-first"
  alias l="eza -CF --color=always --group-directories-first"
else
  # Fallback to traditional ls with colors
  alias la="ls -la $colorflag"
  alias ls="ls $colorflag"
  alias ll="ls -l $colorflag"
  alias l="ls -CF $colorflag"
fi

# Modern alternatives (only if tools are available)
command -v fd >/dev/null 2>&1 && alias find="fd"
command -v rg >/dev/null 2>&1 && alias grep="rg"
command -v bat >/dev/null 2>&1 && alias cat="bat --paging=never"
command -v bat >/dev/null 2>&1 && alias less="bat --paging=always"
command -v delta >/dev/null 2>&1 && alias diff="delta"

# Modern system tools
command -v dust >/dev/null 2>&1 && alias du="dust"
command -v procs >/dev/null 2>&1 && alias ps="procs"
command -v eza >/dev/null 2>&1 && alias tree="eza --tree"
command -v eza >/dev/null 2>&1 && alias lt="eza --tree --level=2"
command -v eza >/dev/null 2>&1 && alias lta="eza --tree --all"

# Help with syntax highlighting
alias bathelp='bat --plain --language=help'

# Additional modern tool aliases
command -v rg >/dev/null 2>&1 && alias rgi="rg -i"  # Case-insensitive ripgrep
command -v rg >/dev/null 2>&1 && alias rgu="rg -uu"  # Unrestricted ripgrep (ignores .gitignore)
command -v fd >/dev/null 2>&1 && alias fdi="fd -i"  # Case-insensitive fd
command -v dust >/dev/null 2>&1 && alias dusts="dust -s"  # Dust with summary
command -v procs >/dev/null 2>&1 && alias procsj="procs --json"  # Procs with JSON output

# =============================================================================
# GIT WORKFLOW
# =============================================================================

# Git aliases for better workflow
alias g="git"
alias gs="git status"
alias ga="git add"
alias gc="git commit"
alias gp="git push"
alias gl="git pull"
alias gd="git diff"
alias gds="git diff --staged"
alias gco="git checkout"
alias gb="git branch"
alias glog="git log --oneline --graph --decorate"

# =============================================================================
# DEVELOPMENT ENVIRONMENT
# =============================================================================

# Chezmoi helpers
alias cm='chezmoi'
alias cma='chezmoi apply'
alias cmd='chezmoi diff'
alias cms='chezmoi status'

# Brew bundle helpers
alias bb='brew-bundle'
alias bbi='brew-bundle install'
alias bbc='brew-bundle check'
alias bbd='brew-bundle dump'
alias edit-packages='edit-packages'
