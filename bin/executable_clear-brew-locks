#!/bin/bash
# =============================================================================
# Clear Homebrew Locks
# Remove all Homebrew lock files to resolve locking issues
# =============================================================================
# Usage: clear-brew-locks
# Example: clear-brew-locks
#
# Description:
#   Removes all Homebrew lock files. Useful when Homebrew gets stuck
#   with lock files that prevent operations.

set -euo pipefail

rm -rf "$(brew --prefix)/var/homebrew/locks"